CREATE VIEW mensagens_nao_lidas AS
SELECT * FROM Mensagem
WHERE data_visualização IS NULL;

CREATE VIEW relações_pendentes AS
SELECT * FROM Relação 
Where estado = 'pendente';

CREATE VIEW publicidade_eficaz AS
SELECT id_publicidade, titulo, alcance
FROM Publicidade
WHERE alcance > 80
ORDER BY alcance DESC;
