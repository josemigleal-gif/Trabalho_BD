--1
CREATE INDEX idx_reacao_contagens 
ON Reação (gosto, não_gosto, adoro);

--2
CREATE INDEX idx_comentario_utilizador_count 
ON Comentário(id_utilizador);

--4
CREATE INDEX idx_mensagem_remetente_destinatario 
ON Mensagem(id_remetente, id_destinatário);

--5
CREATE INDEX idx_publicidade_utilizador_alcance 
ON Publicidade(id_utilizador, alcance);

--8
CREATE INDEX idx_relacao_origem_destino_estado 
ON Relação(id_utilizador_origem, id_utilizador_destino, estado);

--9 
CREATE INDEX idx_publicacao_utilizador_privacidade 
ON Publicação(id_utilizador, privacidade);

--11
CREATE INDEX idx_publicacao_utilizador 
ON Publicação (id_utilizador);
